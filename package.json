{
  "name": "bbws-site",
  "version": "1.4.0",
  "description": "Broadway Beer Wine & Spirits — website",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve",
    "dev": "eleventy --serve --port 8080",
    "postinstall": "node scripts/telemetry.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.20.0",
    "@11ty/eleventy": "^3.0.0",
    "nodemailer": "^6.9.3"
  },
  "engines": {
    "node": ">=18"
  }
}
